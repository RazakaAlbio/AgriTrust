# Agri-Trust — Test Samples Directory
# =====================================
# Place your tomato test images or videos in this folder before running:
#
#   python inference_test.py --source python/tests/your_image.jpg
#   python inference_test.py --source python/tests/              (runs all images in folder)
#   python inference_test.py --source python/tests/video.mp4
#
# Supported formats:
#   Images : .jpg, .jpeg, .png, .bmp, .webp, .tiff
#   Videos : .mp4, .avi, .mov, .mkv, .wmv
#
# Annotated output will be saved to:
#   python/tests/output/
#
# Latency log CSV will be at:
#   python/tests/output/latency_log.csv
